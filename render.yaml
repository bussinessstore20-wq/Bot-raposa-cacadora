services:
  - type: web
    name: bot-raposa-cacadora
    runtime: python

    buildCommand: pip install -r requirements.txt

    startCommand: gunicorn app:app

    envVars:
      - key: BOT_TOKEN
        sync: false

      - key: CHANNEL_USERNAME
        sync: false
